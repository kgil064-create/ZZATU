# 2026-06-06 — Phase 7: 출시 준비 (배포·로그인·약관·탈퇴·정리)

## 한 일
- **Vercel 배포**: GitHub 연결 → 환경변수(Supabase URL/Key) → 프로덕션 배포. `zzatu.vercel.app` 라이브.
- **카카오 로그인 프로덕션화**: Supabase Auth URL Configuration에 프로덕션 도메인 등록(Site URL + Redirect URLs `…/**`).
- **약관/개인정보처리방침**: 두 문서 작성 → `/terms`·`/privacy` 페이지 + 푸터 링크 + 로그인 동의 문구.
- **회원 탈퇴**: 계정·데이터·Storage 완전 삭제. `delete_user()` SECURITY DEFINER 함수 + Server Action(Storage 정리 → rpc → signOut).
- **더미 정리**: 테스트 매물·더미 계정·테스트 이미지 전부 삭제 → 빈 보드.
- **파비콘 + 메타 + OG**: `app/icon.svg`, 전역 metadata, `app/opengraph-image.tsx`(next/og 1200×630).

## 주요 결정
- Vercel **Pro(상업)** — 짜투는 상업 프로젝트라 Hobby(비상업) 부적합. 14일 체험으로 시작.
- Supabase **서울(ap-northeast-2)** + Vercel 함수 **icn1(서울)** 통일 → **국외이전 조항 불필요**.
- 탈퇴는 service role 키 없이 **SECURITY DEFINER 함수**(본인만 삭제, 안전).
- 더미는 hard delete, 본인 계정(고길)은 유지.

## 배운 점 / 함정
- **배포 파이프라인**: 로컬 수정 → commit → push → Vercel 자동 재배포 거쳐야 라이브 반영.
- **OAuth 프로덕션**: 앱 코드 + Supabase URL Configuration에 프로덕션 도메인 등록해야 콜백 복귀(`/**`).
- **마이그레이션**: `.sql`은 기록일 뿐, SQL Editor에서 실행해야 라이브 DB 적용.
- **SECURITY DEFINER + auth.uid()**: owner 권한 실행이지만 auth.uid()는 호출자 기준 → 본인만 삭제.
- **Storage는 DB cascade로 안 지워짐** → 탈퇴/삭제 시 수동 정리 필요.
- **next/og 한글**: 기본 폰트로 한글 불가 → 런타임 Noto Sans KR 서브셋 fetch(프로덕션 정상 확인).

## 남은 것
- (코드 아님) 약관·개인정보 전문 검토, 통신판매중개업 신고/사업자 정보 표시 확인.
- 기능: 찜, 신고(reports 테이블 존재), 알림, 매물별 OG.
- 기술부채: DB 타입 생성, updateItem 트랜잭션화 등.
